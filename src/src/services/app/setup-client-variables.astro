---
const apiHost = import.meta.env.APP_API_HOST;
---

<script define:vars={{ apiHost }}>
  window.App = {
    config: { apiHost }
  };

  console.log('window.App.config', window.App.config);
</script>